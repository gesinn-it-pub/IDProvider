{
  "name": "gesinn-it/id-provider",
  "type": "mediawiki-extension",
  "description": "IDProvider is an extension to MediaWiki that provides (unique) IDs using different ID algorithms.",
  "keywords": [
    "wiki",
    "MediaWiki",
    "extension",
    "Service",
    "ID",
    "UUID"
  ],
  "homepage": "https://github.com/gesinn-it/IDProvider",
  "license": "MIT",
  "authors": [
    {
      "name": "Simon Heimler",
      "email": "heimlersimon@gmail.com",
      "role": "Developer"
    }
  ],
  "minimum-stability": "dev",
  "prefer-stable": true,
  "require": {
    "php": ">=7.4",
    "composer/installers": "^2.3.0"
  },
  "require-dev": {
    "mediawiki/mediawiki-codesniffer": "48.0.0",
    "mediawiki/mediawiki-phan-config": "0.16.0",
    "mediawiki/minus-x": "1.1.3",
    "php-parallel-lint/php-console-highlighter": "1.0.0",
    "php-parallel-lint/php-parallel-lint": "1.4.0"
  },
	"config": {
		"process-timeout": 0,
		"allow-plugins": {
			"composer/installers": true,
			"dealerdirect/phpcodesniffer-composer-installer": true
		}
	},
  "scripts": {
    "test": [
      "@phpunit"
    ],
    "test-coverage": [
      "@phpunit-coverage"
    ],
    "analyze": [
      "@lint",
      "@phpcs"
    ],
    "fix": [
      "@phpcs-fix"
    ],
    "lint": "parallel-lint tests/phpunit",
    "phpcs": "phpcs -ps",
    "phpcs-fix": "phpcbf -p",
    "phan": "phan --allow-polyfill-parser --long-progress-bar",
    "phpunit": [
      "Composer\\Config::disableProcessTimeout",
      "bash -c 'cd ${MW_INSTALL_PATH:-../..} && { [ -f tests/phpunit/generatePHPUnitConfig.php ] && { php tests/phpunit/generatePHPUnitConfig.php >/dev/null 2>&1 || true; [ -f phpunit.xml ] || printf \"%s\" \"<phpunit><!-- generatePHPUnitConfig --></phpunit>\" > phpunit.xml; } || true; } && MEDIAWIKI_HAS_INTEGRATION_TESTS=1 vendor/bin/phpunit -c extensions/IDProvider/phpunit.xml.dist --bootstrap tests/phpunit/bootstrap.php --testdox'"
    ],
    "phpunit-coverage": [
      "Composer\\Config::disableProcessTimeout",
      "bash -c 'cd ${MW_INSTALL_PATH:-../..} && { [ -f tests/phpunit/generatePHPUnitConfig.php ] && { php tests/phpunit/generatePHPUnitConfig.php >/dev/null 2>&1 || true; [ -f phpunit.xml ] || printf \"%s\" \"<phpunit><!-- generatePHPUnitConfig --></phpunit>\" > phpunit.xml; } || true; } && MEDIAWIKI_HAS_INTEGRATION_TESTS=1 vendor/bin/phpunit -c extensions/IDProvider/phpunit.xml.dist --bootstrap tests/phpunit/bootstrap.php --testdox --coverage-text --coverage-html extensions/IDProvider/coverage/php --coverage-clover extensions/IDProvider/coverage/php/coverage.xml'"
    ],
    "minus-x": "minus-x check .",
    "minus-x-fix": "minus-x fix ."
  }
}
